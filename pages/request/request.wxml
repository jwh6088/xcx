<!--pages/request/request.wxml-->
<view class='container'>
  <button bindtap='getData'>请求数据</button>
  <view>
      <view wx:for="{{items}}" wx:key="items">
          <view>{{item.title}}</view>
      </view>
  </view>
</view>
